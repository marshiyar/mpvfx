# GitHub setup

## Repository name

The repository name must be exactly `mpvfx`.

## Repository policy

- Keep the repository aligned with the published release and attribution requirements.
- Protect the default branch with required status checks before merge.
- Do not commit secrets, local project data, or unsigned release artifacts.
